This is the example of state pattern where the state generates the reference of the next state to be loaded and passes it
on the context to load. 

In state pattern, there is a context and its states. 